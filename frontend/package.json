{
  "name": "cyra-frontend-workspace",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "npm run -w frontend dev",
    "build": "npm run -w frontend build && rm -rf \"npm start\" && mkdir -p \"npm start\" && cp -R frontend/out/. \"npm start\"/",
    "start": "npm run -w frontend start",
    "lint": "npm run -w frontend lint"
  }
}
